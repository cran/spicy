% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_apa.R
\name{table_apa}
\alias{table_apa}
\title{Build APA-Style Cross-Tabulation Tables}
\usage{
table_apa(
  data,
  row_vars,
  group_var,
  labels = NULL,
  levels_keep = NULL,
  include_total = TRUE,
  drop_na = TRUE,
  weights = NULL,
  rescale = FALSE,
  correct = FALSE,
  simulate_p = FALSE,
  simulate_B = 2000,
  percent_digits = 1,
  p_digits = 3,
  v_digits = 2,
  assoc_measure = "auto",
  assoc_ci = FALSE,
  decimal_mark = ".",
  output = c("wide", "long", "tinytable", "gt", "flextable", "excel", "clipboard",
    "word"),
  style = c("auto", "raw", "report"),
  indent_text = "  ",
  indent_text_excel_clipboard = "      ",
  add_multilevel_header = TRUE,
  blank_na_wide = FALSE,
  excel_path = NULL,
  excel_sheet = "APA",
  clipboard_delim = "\\t",
  word_path = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{row_vars}{Character vector of variable names to place in rows.}

\item{group_var}{Single character variable name used for columns/groups.}

\item{labels}{Optional character labels for \code{row_vars} (same length).}

\item{levels_keep}{Optional character vector of levels to keep/order for row
modalities. If \code{NULL}, all observed levels are kept.}

\item{include_total}{Logical. If \code{TRUE} (the default), includes a \code{Total} group
when available.}

\item{drop_na}{Logical. If \code{TRUE} (the default), removes rows with \code{NA} in the
row/group variable before each cross-tabulation. If \code{FALSE}, missing values
are displayed as a dedicated \code{"(Missing)"} level.}

\item{weights}{Optional weights. Either \code{NULL} (the default), a numeric vector
of length \code{nrow(data)}, or a single column name in \code{data}.}

\item{rescale}{Logical. If \code{FALSE} (the default), weights are used as-is.
If \code{TRUE}, rescales weights so total weighted N matches raw N.
Passed to \code{spicy::cross_tab()}.}

\item{correct}{Logical. If \code{FALSE} (the default), no continuity correction is
applied. If \code{TRUE}, applies Yates correction in 2x2 chi-squared contexts.
Passed to \code{spicy::cross_tab()}.}

\item{simulate_p}{Logical. If \code{FALSE} (the default), uses asymptotic p-values.
If \code{TRUE}, uses Monte Carlo simulation. Passed to \code{spicy::cross_tab()}.}

\item{simulate_B}{Integer. Number of Monte Carlo replicates when
\code{simulate_p = TRUE}. Defaults to \code{2000}.}

\item{percent_digits}{Number of digits for percentages in report outputs.
Defaults to \code{1}.}

\item{p_digits}{Number of digits for p-values (except \verb{< .001}).
Defaults to \code{3}.}

\item{v_digits}{Number of digits for the association measure. Defaults
to \code{2}.}

\item{assoc_measure}{Passed to \code{\link[=cross_tab]{cross_tab()}}. Which association measure
to report (\code{"auto"}, \code{"cramer_v"}, \code{"phi"}, \code{"gamma"}, \code{"tau_b"},
\code{"tau_c"}, \code{"somers_d"}, \code{"lambda"}, \code{"none"}). Defaults to \code{"auto"}.}

\item{assoc_ci}{Passed to \code{\link[=cross_tab]{cross_tab()}}. If \code{TRUE}, includes the
confidence interval. In data/export formats (\code{wide}, \code{long}, \code{excel},
\code{clipboard}), two extra columns \verb{CI lower} and \verb{CI upper} are added.
In rendered formats (\code{gt}, \code{tinytable}, \code{flextable}, \code{word}), the CI
is shown inline as \code{.14 [.08, .19]} in the association measure column.
Defaults to \code{FALSE}.}

\item{decimal_mark}{Decimal separator (\code{"."} or \code{","}). Defaults to \code{"."}.}

\item{output}{Output format: \code{"wide"} (the default), \code{"long"},
\code{"tinytable"}, \code{"gt"}, \code{"flextable"}, \code{"excel"}, \code{"clipboard"},
\code{"word"}.}

\item{style}{\code{"auto"} (the default) to select by output type, \code{"raw"} for
machine-friendly outputs, \code{"report"} for formatted outputs.}

\item{indent_text}{Prefix used for modality labels in report table building.
Defaults to \code{"  "} (two spaces).}

\item{indent_text_excel_clipboard}{Stronger indentation used in Excel and
clipboard exports. Defaults to six non-breaking spaces.}

\item{add_multilevel_header}{Logical. If \code{TRUE} (the default), merges top
headers in Excel export.}

\item{blank_na_wide}{Logical. If \code{FALSE} (the default), \code{NA} values are kept
as-is in wide raw output. If \code{TRUE}, replaces them with empty strings.}

\item{excel_path}{Path for \code{output = "excel"}. Defaults to \code{NULL}.}

\item{excel_sheet}{Sheet name for Excel export. Defaults to \code{"APA"}.}

\item{clipboard_delim}{Delimiter for clipboard text export. Defaults to \code{"\\t"}.}

\item{word_path}{Path for \code{output = "word"} or optional save path when
\code{output = "flextable"}. Defaults to \code{NULL}.}
}
\value{
Depends on \code{output} and \code{style}:
\itemize{
\item \code{"long"} + \code{"raw"}: long numeric data frame.
\item \code{"wide"} + \code{"raw"}: wide numeric data frame.
\item \code{"long"} + \code{"report"}: long formatted character data frame.
\item \code{"wide"} + \code{"report"}: wide formatted character data frame.
\item \code{"tinytable"}: a \code{tinytable} object.
\item \code{"gt"}: a \code{gt_tbl} object.
\item \code{"flextable"}: a \code{flextable} object.
\item \code{"excel"} / \code{"clipboard"} / \code{"word"}: invisibly returns written
object/path.
}
}
\description{
\code{table_apa()} builds a publication-ready table by crossing one grouping
variable (\code{group_var}) with one or many row variables (\code{row_vars}), using
\code{spicy::cross_tab()} internally.
}
\details{
It supports raw data outputs (\code{wide}, \code{long}) and report-oriented outputs
(\code{tinytable}, \code{flextable}, \code{excel}, \code{clipboard}, \code{word}) with multi-level
headers, p-values, and an association measure.

Optional output engines require suggested packages:
\itemize{
\item \code{tinytable} for \code{output = "tinytable"}
\item \code{gt} for \code{output = "gt"}
\item \code{flextable} + \code{officer} for \code{output = "flextable"}/\code{"word"}
\item \code{openxlsx} for \code{output = "excel"}
\item \code{clipr} for \code{output = "clipboard"}
}
}
\examples{
# Raw long output (machine-friendly)
table_apa(
  data = sochealth,
  row_vars = c("smoking", "physical_activity"),
  group_var = "education",
  labels = c("Current smoker", "Physical activity"),
  output = "long",
  style = "raw"
)

# Raw wide output
table_apa(
  data = sochealth,
  row_vars = c("smoking", "physical_activity"),
  group_var = "education",
  labels = c("Current smoker", "Physical activity"),
  output = "wide",
  style = "raw"
)

# Weighted example
table_apa(
  data = sochealth,
  row_vars = c("smoking", "physical_activity"),
  group_var = "education",
  labels = c("Current smoker", "Physical activity"),
  weights = "weight",
  rescale = TRUE,
  simulate_p = FALSE,
  output = "long",
  style = "raw"
)

\donttest{
# Optional output: tinytable
if (requireNamespace("tinytable", quietly = TRUE)) {
  table_apa(
    data = sochealth,
    row_vars = c("smoking", "physical_activity"),
    group_var = "education",
    labels = c("Current smoker", "Physical activity"),
    output = "tinytable"
  )
}

# Optional output: Excel
if (requireNamespace("openxlsx", quietly = TRUE)) {
  table_apa(
    data = sochealth,
    row_vars = c("smoking", "physical_activity"),
    group_var = "education",
    labels = c("Current smoker", "Physical activity"),
    output = "excel",
    excel_path = tempfile(fileext = ".xlsx")
  )
}
}
}
