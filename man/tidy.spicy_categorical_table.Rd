% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_categorical_print.R
\name{tidy.spicy_categorical_table}
\alias{tidy.spicy_categorical_table}
\alias{glance.spicy_categorical_table}
\title{Tidying methods for a \code{spicy_categorical_table}}
\usage{
\method{tidy}{spicy_categorical_table}(x, ...)

\method{glance}{spicy_categorical_table}(x, ...)
}
\arguments{
\item{x}{A \code{spicy_categorical_table} returned by \code{\link[=table_categorical]{table_categorical()}}.}

\item{...}{Currently ignored. Present for compatibility with the
\code{\link[broom:tidy]{broom::tidy()}} / \code{\link[broom:glance]{broom::glance()}} generics.}
}
\value{
A \code{tbl_df} (when \code{tibble} is installed) or a plain
\code{data.frame}.
}
\description{
Standard \code{\link[broom:tidy]{broom::tidy()}} and \code{\link[broom:glance]{broom::glance()}} interfaces for an
object returned by \code{\link[=table_categorical]{table_categorical()}}. They re-shape the
underlying long-format data (stored on the object as the
\code{"long_data"} attribute) into the two canonical broom views so the
table can be consumed by \code{gtsummary}, \code{modelsummary}, \code{parameters},
and any other tidyverse-stats pipeline.
}
\details{
\code{tidy()} returns one row per \verb{(variable x level)} -- or per
\verb{(variable x level x group)} when \code{by} is used -- with
broom-conventional columns: \code{outcome}, \code{level}, \code{group} (when
applicable), \code{n}, \code{proportion} (the percentage divided by 100).

\code{glance()} returns one row per outcome with the omnibus
chi-squared test (when \code{by} is used) and the requested association
measure: \code{outcome}, \code{test_type} (\code{"chi_squared"}), \code{statistic}
(chi-squared), \code{df}, \code{p.value}, \code{assoc_type}, \code{assoc_value},
\code{assoc_ci_lower}, \code{assoc_ci_upper}, \code{n_total}. Without \code{by}, only
\code{outcome} and \code{n_total} are populated; the other columns are \code{NA}.
}
\seealso{
\code{\link[=as.data.frame.spicy_categorical_table]{as.data.frame.spicy_categorical_table()}} for the raw
wide-format access; \code{\link[=tidy.spicy_continuous_table]{tidy.spicy_continuous_table()}} for the
continuous-descriptive companion.
}
\keyword{internal}
