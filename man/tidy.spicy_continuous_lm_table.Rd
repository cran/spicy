% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_continuous_lm_print.R
\name{tidy.spicy_continuous_lm_table}
\alias{tidy.spicy_continuous_lm_table}
\alias{glance.spicy_continuous_lm_table}
\title{Tidying methods for a \code{spicy_continuous_lm_table}}
\usage{
\method{tidy}{spicy_continuous_lm_table}(x, ...)

\method{glance}{spicy_continuous_lm_table}(x, ...)
}
\arguments{
\item{x}{A \code{spicy_continuous_lm_table} returned by
\code{\link[=table_continuous_lm]{table_continuous_lm()}}.}

\item{...}{Currently ignored. Present for compatibility with the
\code{\link[broom:tidy]{broom::tidy()}} / \code{\link[broom:glance]{broom::glance()}} generics.}
}
\value{
A \code{tbl_df} (when \code{tibble} is installed) or a plain
\code{data.frame}.
}
\description{
Standard \code{\link[broom:tidy]{broom::tidy()}} and \code{\link[broom:glance]{broom::glance()}} interfaces for an
object returned by \code{\link[=table_continuous_lm]{table_continuous_lm()}}. They re-shape the
underlying long-format data into the two canonical broom views so
the table can be consumed by \code{gtsummary}, \code{modelsummary},
\code{parameters}, and any other tidyverse-stats pipeline.
}
\details{
\code{tidy()} returns one row per \strong{estimated parameter} across all
outcomes:
\itemize{
\item One row per fitted level mean (\code{estimate_type = "emmean"})
for categorical predictors, with the level name in \code{term}.
\item One row per \strong{contrast} (\code{estimate_type = "difference"})
when a binary contrast is shown, with \code{term = "<level2> - <level1>"}.
\item One row per \strong{slope} (\code{estimate_type = "slope"}) for
numeric predictors, with \code{term = predictor_label}.
}
Standard broom columns: \code{outcome}, \code{label}, \code{term},
\code{estimate_type}, \code{estimate}, \code{std.error}, \code{conf.low}, \code{conf.high},
\code{statistic}, \code{p.value}. The \code{outcome} column carries the original
variable name; \code{label} carries the human-readable label.

\code{glance()} returns one row per outcome with model-level
statistics: \code{r.squared}, \code{adj.r.squared}, \code{statistic}, \code{df},
\code{df.residual}, \code{p.value}, \code{nobs}, \code{weighted_n}, plus the
effect-size summary \code{es_type}, \code{es_value}, \code{es_ci_lower},
\code{es_ci_upper}, and the test type used for \code{statistic}
(\code{"F"} for categorical predictors, \code{"t"} for numeric ones).
}
\seealso{
\code{\link[=as.data.frame.spicy_continuous_lm_table]{as.data.frame.spicy_continuous_lm_table()}} for the raw
long-format access.
}
\keyword{internal}
