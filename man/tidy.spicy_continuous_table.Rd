% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_continuous_print.R
\name{tidy.spicy_continuous_table}
\alias{tidy.spicy_continuous_table}
\alias{glance.spicy_continuous_table}
\title{Tidying methods for a \code{spicy_continuous_table}}
\usage{
\method{tidy}{spicy_continuous_table}(x, ...)

\method{glance}{spicy_continuous_table}(x, ...)
}
\arguments{
\item{x}{A \code{spicy_continuous_table} returned by \code{\link[=table_continuous]{table_continuous()}}.}

\item{...}{Currently ignored. Present for compatibility with the
\code{\link[broom:tidy]{broom::tidy()}} / \code{\link[broom:glance]{broom::glance()}} generics.}
}
\value{
A \code{tbl_df} (when \code{tibble} is installed) or a plain
\code{data.frame}.
}
\description{
Standard \code{\link[broom:tidy]{broom::tidy()}} and \code{\link[broom:glance]{broom::glance()}} interfaces for an
object returned by \code{\link[=table_continuous]{table_continuous()}}. They re-shape the
underlying long-format data into the two canonical broom views so
the descriptive table can be consumed by \code{gtsummary},
\code{modelsummary}, \code{parameters}, and any other tidyverse-stats
pipeline.
}
\details{
\code{tidy()} returns one row per \verb{(variable x group)} (or per
\code{variable} when \code{by} is not used) with broom-conventional columns:
\code{outcome}, \code{label}, \code{group} (when applicable), \code{estimate} (the
empirical mean), \code{std.error} (\code{sd / sqrt(n)}), \code{conf.low},
\code{conf.high} (the mean confidence interval at \code{ci_level}), \code{n},
\code{min}, \code{max}, \code{sd}. The \code{outcome} column carries the variable name
and \code{label} the human-readable label.

\code{glance()} returns one row per \code{variable} with the omnibus group
comparison (when \code{by} is used) and the requested effect size:
\code{outcome}, \code{label}, \code{test_type}, \code{statistic}, \code{df}, \code{df.residual},
\code{p.value}, \code{es_type}, \code{es_value}, \code{es_ci_lower}, \code{es_ci_upper},
\code{n_total}. Without \code{by}, only \code{outcome}, \code{label}, and \code{n_total}
are populated; the other columns are \code{NA}.
}
\seealso{
\code{\link[=as.data.frame.spicy_continuous_table]{as.data.frame.spicy_continuous_table()}} for the raw
long-format access; \code{\link[=tidy.spicy_continuous_lm_table]{tidy.spicy_continuous_lm_table()}} for the
model-based companion.
}
\keyword{internal}
